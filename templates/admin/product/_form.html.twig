{{ form_start(form) }}
  {{ form_widget(form) }}
  <button style="padding:12px 22px; background:white; border:1px solid #222; border-radius:8px; font-weight:bold;">
    {{ button_label|default('Enregistrer') }}
  </button>
{{ form_end(form) }}
